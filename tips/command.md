# 시스템 설정 변경
## 1. 시스템 폰트 사이즈 
gsettings set org.gnome.desktop.interface text-scaling-factor 1.4(배율)
## 2. 시스템 비밀번호
passwd

# 한글 설치
sudo apt install ibus-hangul -y
* https://basiclike.tistory.com/169

# telegram
sudo snap install telegram-desktop

# git
git config --global user.name "name"
git config --global user.email "email"